import { Metadata } from 'next'
export const metadata: Metadata = { title: 'Quests — Sovereign', description: 'Your tasks and challenges.' }
export default function QuestsLayout({ children }: { children: React.ReactNode }) { return <>{children}</> }
