import { Metadata } from 'next'
export const metadata: Metadata = { title: 'Settings — Sovereign', description: 'Configure the void.' }
export default function SettingsLayout({ children }: { children: React.ReactNode }) { return <>{children}</> }
